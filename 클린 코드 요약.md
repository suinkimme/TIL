# 클린 코드 요약

## 의미 있는 이름을 사용해라

- 변수, 함수, 클래스에 그 목적과 역할을 명확히 나타내는 이름을 부여해라
- 약어나 모호한 단어 사용을 피해라
    - 구체적이고 서술적인 이름을 선택해라

## 함수를 작고 단일 책임을 가지도록 해라

- 각 함수는 한 가지 작업만 수행한다.
- 함수의 길이는 20-30줄 이내로 유지한다.
- 함수의 매개변수 수를 3개 이하로 제한한다.
- ***부수 효과(side effects)**를 최소화하고, 있다면 명확히 표현해라
- ***추상화 수준**을 일관되게 유지한다.
예: 큰 함수를 여러 개의 작은 함수로 분리하여 각각의 역할을 명확히 한다.

### *부수 효과(side effects)를 최소화한다는 것은?

Side Effects는 함수가 자신의 스코프 밖에 있는 상태를 변경하는 것을 말한다.

- 전역 변수 수정
- 매개변수 변경
- 파일 시스템 조작
- 데이터베이스 변경

**나쁜 예시:**

```jsx
let totalAmount = 0;

function calculatePrice(items) {
	let result = 0;
	
	for (const item of items) {
		result += item.price * item.quantity;
		
		// 예: 함수 외부 변수 수정
		totalAmount += item.price * item.quantity;
		
		// 예: 매개변수 수정
		item.processed = true;
	}
}
```

1. 함수명은 단순히 가격을 계산한다고 하지만, 전역 변수를 수정 함
2. 입력 매개변수도 수정하고 있음
3. 이런 부수 효과들이 함수명이나 코드 구조에서 명확히 드러나지 않음

**실제 개발 상황에서의 예시:**

```jsx
// 나쁜 예시:
function getUserData(userId) {
	const user = database.findUser(userId);
	
	// 예상치 못한 부수 효과
	database.updateLastAccessTime(uesrId);
	
	return user;
}

// 좋은 예시:
function getUserData(userId) {
	return database.findUser(userId);
}

function getUserDataAndUpdateAccessTime(userId) {
	const user = database.findUser(userId);
	database.updateLastAccessTime(userId);
	return user;
}
```

부수 효과를 최소화하고 명확히 표현하면:

- 코드의 예측 가능성이 높아진다.
- 테스트가 쉬워진다.
- 디버깅이 간단해진다.
- 재사용성이 향상된다.
- 버그 발생 가능성이 줄어든다.

### *추상화 수준을 일관되게 유지한다는 것은?

**추상화 수준이 일관되지 않은 나쁜 예시:**

```jsx
function processUserOrder(userId, items) {
	// DB에서 사용자 정보 조회
	const db = connectToDatabase();
	const user = db.query(`SELECT * FROM users WHERE id = ${userID}`);
	
	// 재고 확인 - 낮은 추상화 수준(SQL 쿼리 직접 사용)
	let allInStock = true;
	for (const item of items) {
		const result = db.query(`SELECT stock FROM inventory WHERE product_id = ${item.id}`);
		if (result.stock < item.quantity) {
			allInStock = false;
			break;
		}
	}
	
	// 주문 생성 - 높은 추상화 수준(함수 호출)
	if (allInStock) {
		createOrder(user, items);
		
		// 이메일 발송 - 매우 낮은 추상화 수준(SMTP 상세 구현)
		const smtp = new SMTPConnection('smtp.example.com', 587);
		smtp.authenticate('user', 'password');
		smtp.send(user.email, 'Order Confirmation', `Your order of ${items.length} item has been placed.`);
		smtp.close();
		
		return { success: true };
	} else {
		return { success: false, reason: 'Some items are out of stock');
	}
}
```

1. 다양한 추상화 수준이 혼합되어 있음
    - SQL 쿼리 직접 작성 (매우 낮은 수준)
    - 높은 수준의 함수 호출 (`createOrder`)
    - SMTP 연결 구현 상세 (매우 낮은 수준)
2. 코드를 이해하기 어렵고 유지보수가 어려움

**추상화 수준이 일관된 좋은 예시:**

```jsx
function processUserOrder(userId, items) {
  const user = getUserById(userId);
  
  if (areItemsInStock(items)) {
    createOrder(user, items);
    sendOrderConfirmationEmail(user, items);
    return { success: true };
  } else {
    return { success: false, reason: 'Some items are out of stock' };
  }
}

// 세부 구현은 별도 함수로 분리
function getUserById(userId) {
  const db = connectToDatabase();
  return db.query(`SELECT * FROM users WHERE id = ${userId}`);
}

function areItemsInStock(items) {
  const db = connectToDatabase();
  for (const item of items) {
    const result = db.query(`SELECT stock FROM inventory WHERE product_id = ${item.id}`);
    if (result.stock < item.quantity) {
      return false;
    }
  }
  return true;
}

function sendOrderConfirmationEmail(user, items) {
  const subject = 'Order Confirmation';
  const body = `Your order of ${items.length} items has been placed.`;
  sendEmail(user.email, subject, body);
}

function sendEmail(to, subject, body) {
  const smtp = new SMTPConnection('smtp.example.com', 587);
  smtp.authenticate('user', 'password');
  smtp.send(to, subject, body);
  smtp.close();
}
```

1. `processUserOrder` 함수는 일관된 높은 추상화 수준을 유지함
2. 각 세부 작업은 적절한 이름의 함수로 분리됨
3. 각 함수는 단일 책임을 가짐
4. 코드의 흐름을 더 쉽게 이해할 수 있음
5. 테스트와 유지보수가 더 쉬워짐

## 중복 코드 제거

- ***DRY (Don’t Repeat Yourself)** 원칙을 적용한다.
- 반복되는 코드 패턴을 식별하고 공통 함수나 모듈로 추출한다.
- 상속, 컴포지션, 고차 함수 등을 활용하여 코드 재사용성을 높인다.
- 유틸리티 함수나 헬퍼 클래스를 만들어 공통 기능을 모듈화한다.
예: 여러 곳에서 사용되는 날짜 포맷팅 로직을 하나의 유틸리티 함수로 만든다.

### *DRY (Don’t Repeat Yourself)란?

같은 코드(로직, 데이터, 구조 등)를 여러 군데서 반복하지 마라! 같은 의미의 코드를 여러 번 쓰지 말고, 한 번만 정의하고 재사용하라는 원칙이다.

**DRY 원칙 위반:**

```jsx
// 사용자 정보 검증 함수
function validateUserRegistration(user) {
  // 이메일 유효성 검사
  if (!user.email.includes('@')) {
    return { isValid: false, error: '유효하지 않은 이메일 형식입니다.' };
  }
  
  // 비밀번호 길이 검사
  if (user.password.length < 8) {
    return { isValid: false, error: '비밀번호는 8자 이상이어야 합니다.' };
  }
  
  return { isValid: true };
}

// 사용자 정보 업데이트 함수
function validateUserUpdate(userData) {
  // 이메일 유효성 검사 (중복 코드)
  if (!userData.email.includes('@')) {
    return { isValid: false, error: '유효하지 않은 이메일 형식입니다.' };
  }
  
  // 비밀번호 길이 검사 (중복 코드)
  if (userData.password.length < 8) {
    return { isValid: false, error: '비밀번호는 8자 이상이어야 합니다.' };
  }
  
  return { isValid: true };
}
```

1. 이메일과 비밀번호 검증 로직이 두 함수에서 중복된다.
2. 검증 로직이 변경되면 두 곳 모두 수정해야 한다.
3. 실수로 한 곳만 수정하면 일관성이 깨지고 버그가 발생한다.

**DRY 원칙을 적용해 개선된 코드:**

```jsx
// 이메일 유효성 검사 함수
function isValidEmail(email) {
  return email.includes('@');
}

// 비밀번호 유효성 검사 함수
function isValidPassword(password) {
  return password.length >= 8;
}

// 공통 검증 함수
function validateUserData(userData) {
  if (!isValidEmail(userData.email)) {
    return { isValid: false, error: '유효하지 않은 이메일 형식입니다.' };
  }
  
  if (!isValidPassword(userData.password)) {
    return { isValid: false, error: '비밀번호는 8자 이상이어야 합니다.' };
  }
  
  return { isValid: true };
}

// 등록 검증
function validateUserRegistration(user) {
  return validateUserData(user);
  // 필요시 등록에만 필요한 추가 검증 로직 추가 가능
}

// 업데이트 검증
function validateUserUpdate(userData) {
  return validateUserData(userData);
  // 필요시 업데이트에만 필요한 추가 검증 로직 추가 가능
}
```

1. 검증 로직이 한 곳에만 정의됨
2. 검증 기준이 변경되면 한 곳만 수정하면 됨
3. 코드 재사용성 향상
4. 유지보수 용이성 향상
5. 버그 발생 가능성 감소

DRY 원칙은 단순히 코드 줄 수를 줄이는 것이 아니라, 지식과 의도를 코드베이스 내에서 한 번만 표현하도록 하는 것이다. 이를 통해 변경이 필요할 때 한 곳만 수정하면 되므로 유지보수가 훨씬 쉬워진다.

## 주석 대신 자체 설명적인 코드 작성

- 코드 자체로 의도와 동작을 명확히 표현한다.
- 복잡한 알고리즘이나 비즈니스 로직의 경우에만 주석을 사용한다.
- 주석 대신 함수 이름이나 변수 이름으로 의도를 표현한다.

<aside>

주석보다는 자체 설명적인 코드를 선호하는 것에는 동의하지만, 그렇다고 주석 작성 자체를 부정할 필요는 없다고 생각한다.

</aside>

## 일관된 포맷팅 유지

- 팀 내에서 합의된 코딩 스타일 가이드를 따른다.
- 들여쓰기, 중괄호 위치, 공백 사용 등을 일관되게 유지한다.
- 자동 포맷터(예: Prettier)를 사용하여 일관성을 강제한다.
- 파일, 클래스, 함수의 구조를 일관되게 유지한다.
예: 모든 import 문을 파일 상단에 그룹화하고, 알파벳 순으로 정렬한다.

## 복잡한 조건문 단순화

- 깊은 중첩을 피하고, `early return`을 활용한다.
- 복잡한 ***`boolean` 표현식**을 명명된 함수로 추출한다.
- 스위치 문 대신 객체 리터럴이나 Map을 사용한다.
- ***다형성을 활용하여 조건부 로직**을 대체한다.

### *`boolean` 표현식을 명명된 함수로 추출하는 예시

복잡한 if문을 작은 함수로 쪼개는 방법이다.

```jsx
// 복잡한 if문
if (user.memberhsipType === 'premium' || (user.points > 1000 && user.registeredDays > 365)) {
	// 프리미엄 기능 제공
}

// 함수로 호출
function isPremiumUser(user) {
	return user.memberhsipType === 'premium' || (user.points > 1000 && user.registeredDays > 365);
}

if (isPremiumUser(user)) {
	// 프리미엄 기능 제공
}
```

### *다형성을 화용한 조건부 로직 예시

다형성은 객체지향 프로그래밍에서 같은 메소트 이름이지만 다른 동작을 하는 여러 클래스를 만들어 사용하는 방식이다.

예: 여러 종류의 결제 방법(신용카드, 페이팔, 계좌이체)를 처리해야 한다면:

```jsx
// 일반적인 if-else 방식
function processPayment(paymentMethod, amount) {
	if (paymentMethod === 'credit_card') {
		// 신용카드 결제 처리
	} else if (paymentMethod === 'paypal') {
		// 페이팔 결제 처리
	} else if (paymentMethod === 'bank_transfer') {
		// 계좌이체 처리
	}
}

// 다형성 활용 방식
// 결제 처리기 인터페이스
class PaymentProcessor {
	process(amount) {
		// 추상 메소드 (구현 필요)
	}
}

// 신용카드 결제 처리기
class CreditCardProcessor extends PaymentProcessor {
	process(amount) {
		// 신용카드 결제 처리
		console.log(`신용카드로 ${amount}원 결제`);
	}
}

// 페이팔 결제 처리기
class PaypalProcessor extends PaymentProcessor {
	process(amount) {
		// 페이팔 결제 처리
		console.log(`페이팔로 ${amount}원 결제`);
	}
}

// 사용 예시
function processPayment(processor, amount) {
	processor.process(amount);
}

// 신용카드 결제
processPayment(new CreditCardProcessor(), 50000);
// 페이팔 결제
processPayment(new PaypalProcessor(), 30000);
```

변화에 더 유연하게 대응하고 개발 속도와 안정성을 높이는 방법이고, 조건부 로직을 “완전히 제거” 하기보다는 “재배치”하고 “캡슐화”하는 것이다.

마지막 사용 예를 보면 두 결제 방식에 따라 조건문을 사용해야함을 알 수 있다.

## 적절한 추상화 수준 유지

- ***비즈니스 로직과 저수준 구현 세부사항을 분리**한다.
- ***인터페이스와 구현을 명확히 구분**한다.
- 과도한 추상화를 피하고, 필요한 만큼만 추상화한다.
- 동일한 수준의 추상화를 한 곳에서 다룬다.
- ***SOLID 원칙**을 적용하여 모듈 간 결합도를 낮추고 응집도를 높인다.

### *비즈니스 로직과 저수준 구현 세부사항 분리 예시

추상화 수준 분리는 “무엇을 할 것인가(What)”와 “어떻게 할 것인가(How)”를 구분하는 것이다.

- **비즈니스 로직(높은 추상화):** 애플리케이션의 목적과 흐름을 표현하는 코드
- **구현 세부사항(낮은 추상화):** 그 목적을 달성하기 위한 기술적 세부 구현

**나쁜 예시:**

```jsx
function transferMoney(fromAccountId, toAccountId, amount) {
  // 데이터베이스 연결 설정 (저수준 구현 세부사항)
  const connection = mysql.createConnection({...});
  
  // 계좌 잔액 확인 (SQL 직접 사용 - 저수준 구현)
  const query = `SELECT balance FROM accounts WHERE id = ${fromAccountId}`;
  const result = connection.query(query);
  const balance = result[0].balance;
  
  // 비즈니스 로직 (잔액 검증)
  if (balance < amount) {
    // 에러 처리 (이메일 전송 - 저수준 구현)
    const emailService = new EmailService();
    emailService.sendEmail('insufficient_funds', fromAccountId);
    return false;
  }
  
  // 송금 처리 (SQL 직접 사용 - 저수준 구현)
  connection.query(`UPDATE accounts SET balance = balance - ${amount} WHERE id = ${fromAccountId}`);
  connection.query(`UPDATE accounts SET balance = balance + ${amount} WHERE id = ${toAccountId}`);
  
  return true;
}
```

**좋은 예시:**

```jsx
// 비즈니스 로직 (높은 추상화 수준)
function transferMoney(fromAccountId, toAccountId, amount) {
  // 계좌 잔액 확인 (추상화된 메서드 호출)
  const balance = accountRepository.getBalance(fromAccountId);
  
  // 비즈니스 규칙 검증
  if (balance < amount) {
    notificationService.notifyInsufficientFunds(fromAccountId);
    return false;
  }
  
  // 송금 처리 (추상화된 메서드 호출)
  accountRepository.decreaseBalance(fromAccountId, amount);
  accountRepository.increaseBalance(toAccountId, amount);
  
  return true;
}

// 저수준 구현 세부사항 (별도 모듈/파일에 구현)
const accountRepository = {
  getBalance(accountId) {
    // 데이터베이스 연결 및 SQL 쿼리 실행
    const connection = mysql.createConnection({...});
    const result = connection.query('SELECT balance FROM accounts WHERE id = ?', [accountId]);
    return result[0].balance;
  },
  
  decreaseBalance(accountId, amount) {
    const connection = mysql.createConnection({...});
    connection.query('UPDATE accounts SET balance = balance - ? WHERE id = ?', [amount, accountId]);
  },
  
  increaseBalance(accountId, amount) {
    const connection = mysql.createConnection({...});
    connection.query('UPDATE accounts SET balance = balance + ? WHERE id = ?', [amount, accountId]);
  }
};

const notificationService = {
  notifyInsufficientFunds(accountId) {
    // 이메일 전송 구현
    const emailService = new EmailService();
    emailService.sendEmail('insufficient_funds', accountId);
  }
};
```

이렇게 분리하면 비즈니스 로직은 변경되지 않으면서 저수준 구현(예: MySQL에서 MongDB로 변경)만 독립적으로 수정할 수 있다.

### *인터페이스와 구현의 명확한 구분이란?

`*비즈니스 로직과 저수준 구현 세부사항 분리 예시`에서도 다룬 내용이지만, “무엇을 한다”와 “어떻게 한다”를 분리하라는 뜻이다.

- 인터페이스(Interface)
    - 무엇을 할 수 있는가에 대한 명세
    - 예: “이 객체는 `save()` 기능을 제공한다.”
- 구현(Implementation)
    - 그 기능을 실제로 어떻게 수행하는가에 대한 구체적 내용.
        - 예: “이 `save()`는 DB에 저장한다.” 혹은 “파일로 저장한다.”

<aside>
💡

**왜 이걸 구분해야 할까?**

1. 유연성 확보
→ 구현을 바꿔도, 인터페이스만 유지되면 코드 전체를 바꿀 필요가 없다.
2. 테스트 용이
→ 실제 구현 대신 인터페이스만 써서 가짜(mock) 객체로 테스트 가능
3. 의존성 역전 원칙(DIP) 같은 SOLID 원칙 준수
→ 상위 모듈이 하위 구현에 의존하지 않고, 인터페이스에만 의존하게 함.
</aside>

### *SOLID 원칙이란?

SOLID는 객체 지향 설계의 핵심 원칙 중 하나로, 코드가 유지보수하기 쉽고, 확장하기 쉬운 구조가 되도록 도와주는 다섯 가지 원칙이다.

### S - 단일 책임 원칙 (SRP: Single Responsibility Principle)

> 클래스는 하나의 책임만 가져야 한다.
> 

하나의 클래스는 한 가지 일만 해야 하고, 그 일에 대해서만 변경이 일어나야 한다.

**예시:**

`UserManager` 클래스가 로그인도하고, DB 저장도 하고, 이메일도 보낸다면?
→ 역할 분리: `LoginService`, `UserRepository`, `EmailSender`로 나눠야 한다.

---

### O - 개방-폐쇄 원칙 (OCP: Open/Closed Principle)

> 확장에는 열려 있고, 변경에는 닫혀 있어야 한다.
> 

기존 코드를 고치지 않고 새 기능을 추가할 수 있어야 한다.

**예시:**

결제 방식이 추가될 때 `if (type === '카드') ...` 이런 조건문을 늘리기 보다.
→ `PaymentProcessor` 인터페이스 만들어서 새 클래스를 추가하는 식으로 확장 한다.

---

### L - 리스코프 치환 원칙 (LSP: Liskov Substitution Principle)

> 자식 클래스는 언제나 부모 클래스를 대체할 수 있어야 한다.
> 

상속을 받았으면 부모 클래스가 쓰이는 자리에 문제없이 들어갈 수 있어야 한다.

**예시:**

부모가 `Bird`고, 자식이 `Penguin`인데 `fly()`를 가지고 있다면… 펭귄은 날지 못한다.
→ 이런 설계는 LSP 위반 (펭귄은 `Bird`이긴 해도 `fly()`를 가질 순 없다.)

---

### I - 인터페이스 분리 원치 (ISP: Interface Segregation Principle)

> 클라이언트 자신이 사용하지 않는 메서드에 의존하지 않아야 한다.
> 

너무 많은 기능을 가진 거대한 인터페이스를 나누라는 것

**예시:**

`Printer` 인터페이스에 `print()`, `scan()`, `fax()` 다 있으면
→ 어떤 프린터는 스캔/팩스 기능 없을 수도 있으니, 인터페이스를 분리해야 한다.

---

### D - 의존성 역전 원칙 (DIP: Dependency Inversion Principle)

> 상위 모듈은 하위 모듈에 의존하면 안 된다. 둘 다 추상화해 의존해야 한다.
> 

구체적인 클래스보단 인터페이스나 추상 클래스에 의존하라는 뜻

**예시:**

`OrderService`가 `MysqlRepository`를 사용하면 안 되고,

→ `UserRepository`라는 인터페이스를 쓰고, 구현은 외부에서 주입(injection)해야 한다.

```jsx
// DIP를 위반한 코드
// 구체 클래스
class EmailService {
  sendEmail(userEmail, message) {
    console.log(`Sending email to ${userEmail}: ${message}`);
  }
}

class UserService {
  constructor() {
    this.emailService = new EmailService(); // ❌ 구체 클래스에 직접 의존
  }

  register(user) {
    console.log(`Registering user: ${user.name}`);
    this.emailService.sendEmail(user.email, 'Welcome!');
  }
}

// 사용
const userService = new UserService();
userService.register({ name: 'Alice', email: 'alice@example.com' });

// DIP를 지킨 코드 (추상화에 의존)
// 추상 인터페이스 역할을 하는 프로토콜
class MessageService {
  send(recipient, message) {
    throw new Error('send() must be implemented');
  }
}

// EmailService는 추상화(MessageService)를 구현
class EmailService extends MessageService {
  send(recipient, message) {
    console.log(`Sending email to ${recipient}: ${message}`);
  }
}

// 다른 구현 예시: SMS
class SmsService extends MessageService {
  send(recipient, message) {
    console.log(`Sending SMS to ${recipient}: ${message}`);
  }
}

class UserService {
  constructor(messageService) {
    this.messageService = messageService; // ✅ 추상화에 의존
  }

  register(user) {
    console.log(`Registering user: ${user.name}`);
    this.messageService.send(user.email, 'Welcome!');
  }
}

// 사용
const emailService = new EmailService();
const userService = new UserService(emailService); // 의존성 주입

userService.register({ name: 'Alice', email: 'alice@example.com' });
```

- `UserService`는 `EmailService`, `SmsService` 등 구현이 어떤 건지 몰라도 동작 가능
- 변경에 강하고, 테스트도 쉬움 (예: 테스트용 MockMessageService 만들기 쉬움)
    
    
    | **원칙** | **핵심 요약** |
    | --- | --- |
    | SRP | 클래스는 하나의 책임만 가져야 한다. |
    | OCP | 기능은 추가할 수 있어야 하고, 기존 코드는 안 건드려야 한다. |
    | LSP | 자식 클래스는 부모를 대체할 수 있어야 한다. |
    | ISP | 인터페이스는 작게, 필요한 것만 가져야 한다. |
    | DIP | 구체적인 구현이 아니라 추상화에 의존하라. |